# pathping
Think of pathping as a hybrid of the first two commands. It takes a few minutes to run because it calculates statistics over time to find exactly which node is losing data.

```cmd
pathping google.com
```
